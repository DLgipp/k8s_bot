from bot import main

def test_random_number_in_range():
    low, high = 1, 10
    # допустим, у тебя есть функция, которая генерирует число (если нет — сделаем)
    # пример, если она есть:
    # result = main.generate_random_number(low, high)
    # assert low <= result <= high
    pass  # пока заглушка
